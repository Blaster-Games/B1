syntax = "proto3";

package Protocol;
option csharp_namespace = "Google.Protobuf.Protocol";

enum EGameMode {
    MODE_NONE = 0;
    MODE_DEATHMATCH = 1;
    MODE_TEAMDEATHMATCH = 2;
}

enum ETeamType {
    TEAM_NONE = 0;
    TEAM_RED = 1;
    TEAM_BLUE = 2;
}

enum ERoomState {
    STATE_WAITING = 0;
    STATE_PLAYING = 1;
    STATE_TERMINATED = 2;
}